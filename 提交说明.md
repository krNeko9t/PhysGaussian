### 我到底改了什么（按“你想要的能力”倒推）

你的目标是：**只让少数实例（toy/杯子）做物理模拟**，其他实例（wall/floor/桌子）**只显示或当边界**；同时 **没有可靠 cameras.json**、场景中心也不在 \((0,0,0)\) 也要能看结果。为此我做了 5 类改动。

---

## 1) 把“对象用途”显式化：`simulate` vs `render_only`
**做了什么**
- config 的 `objects` 里新增了 `mode` 字段：`simulate` / `render_only`（`collider_only` 预留）。
- `pipeline.py` 在 multi-object 模式下按 `mode` 分流：
  - `simulate`：进入物理后端（你的 toy/杯子）
  - `render_only`：只参与渲染合成（你的 wall/floor/背景）

**为什么这样干**
- 以前 `pipeline.py` 的 multi-object 只有“进模拟的对象”，剩下的“静态”来源很绕（只有 shared PLY 里没选中的那部分）。显式 `mode` 后，你可以**直接把分割后的 N 个 PLY 按用途挂上去**，更可控、更可读。

---

## 2) 解决“只模拟 toy 会打乱与墙/地相对位置”的坐标问题：`transform_reference`
**做了什么**
- config 新增 `transform_reference`：
  - `"shared_ply"`：用命令行 `--ply_path`（例如 `all.ply`）的 bbox 计算 world→MPM 归一化变换
  - `{"ply_path": ...}`：用指定 PLY 做参考
  - 默认 None：保持旧行为（用“模拟粒子”算变换）
- `pipeline.py` 里：**先用 reference 算 transform（scale/center）**，再把 simulate 粒子用同一套变换送进后端。

**为什么这样干**
- 旧逻辑里，transform 是按“参与模拟的粒子”算的；如果只模拟 toy，整个坐标系就“围着 toy 重标定”，那 wall/floor（即使你渲染出来）**也不再与模拟物体对齐**。用 `all.ply` 做 reference 就能保持 toy、wall、floor 的原始相对关系。

---

## 3) 让 wall/floor 当平面边界更容易：`boundary_conditions.space="world"`
**做了什么**
- `boundary_conditions` 里支持 `space: "world"`（仅对 `surface_collider` 做转换）：你用 world 坐标写 plane 的 `point/normal`，`pipeline.py` 会自动把它映射到 MPM 坐标再交给后端。

**为什么这样干**
- 旧配置要求你手算“归一化后的 MPM 坐标”，很容易写错。你现在的 wall/floor PLY 都在 world 坐标系里，最自然就是直接写 world-space 平面。

---

## 4) 降低 camera 依赖：程序化相机 `--camera_mode orbit/fixed`
**做了什么**
- `pipeline.py` 新增 `--camera_mode json|orbit|fixed`：
  - `json`：旧行为，读 `cameras.json`
  - `orbit`：不需要 `cameras.json`，用 config 的 `width/height` + `fx/fy` 或 `fov*_deg`，围绕场景中心绕一圈看
  - `fixed`：同理，但用固定外参
- `physics_sim/renderer/gs_renderer.py` 增加对应的 `build_camera_orbit()` / `build_camera_fixed()`。

**为什么这样干**
- 你的真实应用“拿不到好 camera”，但你仍需要可视化结果。程序化相机至少能让你**稳定看见物体是否落地、是否穿模、是否越界**，用于调物理参数/边界。

---

## 5) 让“没装齐渲染/物理依赖”也能跑通流程：`--no_render` + 可选导入
**做了什么**
- `--no_render`：只跑模拟（或 render-only backend），不触发高斯光栅化依赖；输出最终状态 `final_state.npz`。
- `diff_gaussian_rasterization` 改为渲染时才导入（否则 `--no_render` 也会因缺包直接崩）。
- Taichi/particle_filling 也改为“用到才需要”，避免刚体/无填充场景硬依赖。

**为什么这样干**
- 你当前环境里缺 `diff_gaussian_rasterization`，且安装 `warp_lang` 还遇到磁盘 quota。这个改动保证你至少可以**先把数据流（多 PLY 组装、transform_reference、world-space collider）验证通**，不被环境卡死。

---

### bench_manual 我是怎么按你的意图落地的
新增了一个示例配置：`config/bench_manual_rigid_config.json`，表达的就是：
- `toy`：`simulate`（物理主体）
- `wall/floor`：`render_only`（只看）
- `boundary_conditions`：两个 world-space 平面（地面、墙）
- `transform_reference: "shared_ply"`（用 `all.ply` 保持相对位置）
- 程序化 orbit 相机参数

另外你这批 PLY 只有 `f_dc_0..2`（没有高阶 SH 特征），所以渲染/加载要用 `--sh_degree 0`，否则会断言失败。

---

如果你接下来要的就是“wall/floor 不用手填平面参数，自动从 `floor.ply`/`wall.ply` 拟合出 plane”，这属于在现有结构上加一个很干净的小模块（不会再堆 if/else）。你点头我再给你一个具体设计接口（Ask 模式下我只能给方案，不直接改代码）。